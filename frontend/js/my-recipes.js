document.addEventListener('DOMContentLoaded',async()=>{try{const data=await FoodLab.api('/api/users/me/recipes');const render=status=>{const items=data.items.filter(r=>status==='draft'?r.status==='draft':r.status==='published');document.querySelector('#my-recipes').innerHTML=items.length?items.map(r=>`<div class="recipe-manage-row"><div class="recipe-manage-info"><strong>${FoodLab.escape(r.title)}</strong><small>${FoodLab.escape(r.description||'暂无简介')} · ${FoodLab.escape(r.cuisine||'未分类')}</small></div><div class="recipe-manage-actions"><span class="status-pill">${status==='draft'?'草稿':'已发布'}</span><a class="btn secondary" href="/publish.html?edit=${r.id}">编辑</a><button class="btn ghost" data-delete="${r.id}">删除</button>${status==='draft'?`<button class="btn" data-publish="${r.id}">发布</button>`:''}</div></div>`).join(''):'<div class="empty">这个列表暂时没有菜谱。</div>';document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=async()=>{if(!confirm('确认删除这道菜谱吗？'))return;await FoodLab.api('/api/recipes/'+b.dataset.delete,{method:'DELETE'});const item=data.items.find(x=>String(x.id)===String(b.dataset.delete));if(item)item.status='deleted';render(status);FoodLab.toast('菜谱已删除')});document.querySelectorAll('[data-publish]').forEach(b=>b.onclick=async()=>{await FoodLab.api('/api/recipes/'+b.dataset.publish,{method:'PUT',body:{status:'published'}});const item=data.items.find(x=>String(x.id)===String(b.dataset.publish));if(item)item.status='published';render('draft');FoodLab.toast('菜谱已发布')})};document.querySelectorAll('[data-tab]').forEach(tab=>tab.onclick=()=>{document.querySelectorAll('[data-tab]').forEach(x=>x.classList.remove('active'));tab.classList.add('active');render(tab.dataset.tab)});render('published')}catch(e){FoodLab.toast(e.message);setTimeout(()=>location.href='/login.html',700)}});
