document.addEventListener('DOMContentLoaded',async()=>{const id=new URLSearchParams(location.search).get('id');try{const me=await FoodLab.loadUser();const uid=id||(me&&me.id);if(!uid)throw Error('请先登录');const d=await FoodLab.api('/api/users/'+uid);const own=me&&String(me.id)===String(uid);const recipeData=own?await FoodLab.api('/api/users/me/recipes'):{items:d.recipes};const recipes=recipeData.items||[];document.querySelector('#profile-page').innerHTML=`<section class="profile-hero panel"><div class="profile-avatar">${FoodLab.escape(d.user.username.slice(0,1))}</div><div class="profile-copy"><span class="eyebrow">Personal profile</span><h1>${FoodLab.escape(d.user.username)}</h1><p class="muted">${FoodLab.escape(d.user.bio||'这个人还没有写个人介绍。')}</p><div class="tags profile-interests">${(d.user.interests||[]).map(t=>`<span class="tag">${FoodLab.escape(t)}</span>`).join('')||'<span class="muted">还没有设置兴趣标签</span>'}</div><div class="stats"><span>发布 ${d.counts.recipes}</span><span>粉丝 ${d.user.followers||0}</span><span>获赞 ${d.counts.likes}</span></div></div></section><div id="my-recipes" class="section-head" style="margin-top:40px"><h2>${own?'我的菜谱':'公开菜谱'}</h2></div><div class="grid">${recipes.filter(item=>item.status==='published'||!own).map(item=>FoodLab.renderCard(item)).join('')||'<div class="empty">还没有已发布菜谱。</div>'}</div>`}catch(e){FoodLab.toast(e.message);setTimeout(()=>location.href='/login.html',800)}});
